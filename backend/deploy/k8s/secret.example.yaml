# Template — DO NOT commit real values. Create from a secret store (Vault, AWS
# Secrets Manager via External Secrets) in production. stringData is plaintext
# for convenience; kubectl encodes it.
apiVersion: v1
kind: Secret
metadata:
  name: opotrade-gateway-secret
  labels:
    app: opotrade-gateway
type: Opaque
stringData:
  JWT_SECRET_KEY: "__change_me__"
  MT5_LOGIN: "7898"
  MT5_PASSWORD: "__change_me__"
  POSTGRES_DSN: "postgres://opo:__change_me__@postgres:5432/trading_ops?sslmode=require"
  TIMESCALE_DSN: "postgres://opo:__change_me__@timescale:5432/market?sslmode=require"
  REDIS_ADDRS: "redis:6379"
  REDIS_PASSWORD: "__change_me__"
  NATS_URL: "nats://nats:4222"
